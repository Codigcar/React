import ReactDOM from 'react-dom';

import { JournalApp } from './JournalApp';
// instalar: npm install node-sass
import './styles/style.scss';

ReactDOM.render(
  <JournalApp />,
  document.getElementById('root')
);

